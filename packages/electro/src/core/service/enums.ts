export enum ServiceScope {
    PRIVATE = "private",
    INTERNAL = "internal",
    EXPOSED = "exposed",
}

export enum ServiceStatus {
    REGISTERED = "registered",
    ACTIVE = "active",
    DESTROYED = "destroyed",
}
